# PostgreSQL connection
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=
DB_NAME=stampy

# Optional: if using a connection string instead
DATABASE_URL=postgresql://username:password@localhost:5432/stampy